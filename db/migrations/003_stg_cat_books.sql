-- ===========================================================================
-- 003. 카탈로그 적재용 임시 테이블
--
-- cat_books.jsonl(배송 #5, 579,811행)을 가공 없이 그대로 붓는 자리.
-- v_books로 변환하는 단계에서 손볼 것(book_id 임시 발급, price 이상치 제외
-- 등)을 처리하므로, 여기엔 원본을 있는 그대로 유지한다.
--
-- 운영에는 없는 테이블이다 — 개발용 카탈로그 시딩이 끝나면 지워도 된다.
-- ===========================================================================

CREATE TABLE IF NOT EXISTS stg_cat_books (
    data jsonb NOT NULL
);
