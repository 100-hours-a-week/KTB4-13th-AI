"""인기 점수 — v_book_popularity 를 읽는 모든 곳이 같은 식을 쓰게 한 곳에 둔다.

① 검색의 인기순, ③ 챗봇 후보 채점, ④ 피드의 인기 항, 신규 사용자용 목록이 대상이다.
각자 다른 식을 쓰면 화면마다 인기 순위가 달라진다.

    인기 점수 = 판매 수

- 백엔드가 인기 점수를 맡기로 했고, MVP 는 판매량 기준이다(BE #68, AI #56). 백엔드 상품 목록의
  인기도순과 같은 값·같은 순서가 나오도록 판매 수를 그대로 쓴다(#186).
- 행이 없는 책은 0점이다(명세).
- ④ 인기 항은 이 값을 카탈로그 최고값으로 나눠 0–1 로 바꾼다. 판매 수가 쏠려 있으면 소수의 베스트셀러만
  높고 나머지는 0 에 가깝다.

리뷰를 반영한 점수는 백엔드가 공식을 정해 복제 표에 칼럼으로 넘겨주면 그 칼럼을 읽도록 바꾼다(AI #56).
"""


def score_sql(alias: str = "p") -> str:
    """인기 점수를 계산하는 SQL 식. `alias` 는 LEFT JOIN 한 v_book_popularity 의 별칭이다.

    행이 없는 책(LEFT JOIN 결과가 NULL)은 0 이 된다.
    """
    return f"coalesce({alias}.sales, 0)"
